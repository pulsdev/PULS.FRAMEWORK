public class GlobalPermissions
{
    /// <summary>
    /// If an endpoint dose not need any special permission (No Permission)
    /// </summary>
    public const string AuthenticateOnly = nameof(AuthenticateOnly);
}